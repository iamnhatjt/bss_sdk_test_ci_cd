<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="20dp" />
            <solid android:color="@color/md_theme_dark_onPrimary" />
            <!--            <stroke android:color="@color/transparent" android:width="6dp" />-->
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <scale android:scaleWidth="100%" android:useIntrinsicSizeAsMinimum="true">
            <shape>
                <corners android:radius="20dp" />
                <solid android:color="@color/md_theme_dark_surfaceTint" />
            </shape>
        </scale>
    </item>
</layer-list>